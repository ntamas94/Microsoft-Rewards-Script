# Copy to .env next to compose.yaml.
# Where the Edge profile is stored on the host. Keep it on the data disk:
# it holds the signed-in Edge profile, so it must survive container rebuilds.
EDGE_PROFILE_DIR=/mnt/hdd/edge-browse-minutes/profile
